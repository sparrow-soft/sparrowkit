Your sign-in code is <%= @code %>

It expires in <%= (SparrowAuth::OneTimeCode::LIFETIME / 60).to_i %> minutes and can
only be used in the browser that asked for it.

If you did not ask to sign in, you can ignore this message. Nobody can use this
code without also having your browser open.
